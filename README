Commands to sync the repository
$ repo init -u git://github.com/SomethingJelly/android.git -b sj4.2.1
$ repo sync -j#

After the repository is synced, run in source directory:
$ . build/envsetup.sh
$ lunch
Pick device to build for
$ make -j# && make otapackage -j#

(# is how many cores your machine has x2)
